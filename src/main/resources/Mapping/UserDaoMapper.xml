<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace是接口UserDao的路径 -->
<mapper namespace="com.comfirstdemo.dao.UserDao">
    <!--id对应select查询方法的方法名 -->
    <select id="queryAllUser" resultType="com.comfirstdemo.bean.User">
        select * from user
    </select>
    <select id="queryUserById" parameterType="integer" resultType="com.comfirstdemo.bean.User" >
        select * from user where id = #{id}
    </select>
    <insert id="addUser" parameterType="user">
        insert into user (name,password,email,address) values(#{name},#{password},#{email},#{address})
    </insert>
    <delete id="deleteUserById" parameterType="integer">
        delete from user where id = #{id}
    </delete>
    <update id="updateUser" parameterType="user">
        update user set name = #{name},password = #{password},email = #{email},address = #{address} where id = #{id}
    </update>
</mapper>